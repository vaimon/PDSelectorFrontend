# Backend origin. Leave empty for prod: the frontend and API are same-origin under
# teams.pd-mmcs.ru (edge nginx serves static and proxies /api + /oauth2 to the backend).
# For local dev the Spring backend runs cross-origin on :8080 — point at it:
VITE_BACKEND_URL=http://localhost:8080
